{
   env: {
       - jest: true
       - node: true
       + node: false
   }
   extends: airbnb-base
 + name: Node CI
   parserOptions: {
         ecmaVersion: latest
         sourceType: module
   }
 + rules: {
         no-console: 0
         import/extensions: 0
   }
}